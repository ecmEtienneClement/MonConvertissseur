<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="etienne.clement.myconvertion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/my_icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/my_icone"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyConvertion">
        <activity android:name=".vue.MainActivity3" />
        <activity android:name=".vue.MainActivity2" />
        <activity android:name=".vue.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>